"""report — rendered outputs at the top of the stack: the offline single-file HTML
dashboard (``dashboard``) and the offline Markdown digest of triggered signals
(``digest``). Pulls from every lower slice; nothing imports report except the CLI.
"""
