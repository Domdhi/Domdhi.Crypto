"""agent — the in-engine agent-interface seam: ``context`` assembles the signals +
position + factor-menu snapshot an LLM agent reasons over. This is the boundary the
separate ``domdhi_crypto_mcp`` package consumes; keeping it here (not in mcp) means the
engine's own ``report`` slice can build context without depending on the agent server.
"""
